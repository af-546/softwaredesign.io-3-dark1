/*
 * TRUST BADGE ROW
 *
 * Returns null until real review profiles and insurance verification are wired.
 * Wire each badge `href` to the real review profile before production launch:
 *  - Google Business Profile (Google Reviews)
 *  - Trustpilot business profile
 *  - BBB Accreditation profile (only if accredited)
 *  - Verify insurance & bonding before displaying that badge
 */

export default function TrustBadgeRow() {
  return null;
}
